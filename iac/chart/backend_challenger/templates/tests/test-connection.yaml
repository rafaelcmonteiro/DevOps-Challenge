apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "backend_challenger.fullname" . }}-test-connection"
  labels:
    {{- include "backend_challenger.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "backend_challenger.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
