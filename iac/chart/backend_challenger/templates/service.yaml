apiVersion: v1
kind: Service
metadata:
  name: {{ include "backend_challenger.fullname" . }}
  labels:
    {{- include "backend_challenger.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "backend_challenger.selectorLabels" . | nindent 4 }}
